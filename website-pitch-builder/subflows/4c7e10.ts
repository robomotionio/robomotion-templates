import { subflow, Credential, Message } from '@robomotion/sdk';

subflow.create('Gather The Material', (f) => {
  f.node('81a2b3', 'Core.Flow.Begin', 'Begin', {})
    .then('92b3c4', 'Core.Vault.GetItem', 'Get Scrape.do Token', { optCredentials: Credential({ vaultId: '_', itemId: '_' }) })
    .then('a3c4d5', 'Core.Programming.Function', 'Build Place URL', { func: '\nvar token = msg.credentials && msg.credentials.value;\nif (!token) {\n  throw new Error(\'Scrape.do token is empty in the vault item\');\n}\n\nvar w = msg.winner || {};\nvar param = \'\';\nvar id = \'\';\nif (w.place_id) {\n  param = \'place_id\';\n  id = w.place_id;\n} else if (w.data_id) {\n  param = \'data_id\';\n  id = w.data_id;\n}\nif (!id) {\n  throw new Error(\'The winning place carries no place_id or data_id to look up\');\n}\n\nmsg.place_param = param;\nmsg.place_key = id;\nmsg.place_url = \'https://api.scrape.do/plugin/google/maps/place\' +\n  \'?token=\' + encodeURIComponent(token) +\n  \'&\' + param + \'=\' + encodeURIComponent(id) +\n  \'&hl=en\';\nreturn msg;\n' })
    .then('b4d5e6', 'Core.Net.HttpRequest', 'Get Place Details', {
    outBody: Message('place_raw'),
    outStatus: Message('place_status'),
    optMethod: 'get',
    optUrl: Message('place_url'),
    optTimeout: 90
  })
    .then('c5e6f7', 'Core.Programming.Function', 'Build Reviews URL', { func: '\nvar token = msg.credentials && msg.credentials.value;\nvar w = msg.winner || {};\n\nvar param = \'data_id\';\nvar id = w.data_id || \'\';\nif (!id) {\n  param = \'place_id\';\n  id = w.place_id || \'\';\n}\nif (!id) {\n  throw new Error(\'The winning place carries no id to fetch reviews with\');\n}\n\nmsg.reviews_url = \'https://api.scrape.do/plugin/google/maps/reviews\' +\n  \'?token=\' + encodeURIComponent(token) +\n  \'&\' + param + \'=\' + encodeURIComponent(id) +\n  \'&hl=en\' +\n  \'&sort_by=highest_rating\' +\n  \'&num=10\';\nreturn msg;\n' })
    .then('d6f708', 'Core.Net.HttpRequest', 'Get Ten Best Reviews', {
    outBody: Message('reviews_raw'),
    outStatus: Message('reviews_status'),
    optMethod: 'get',
    optUrl: Message('reviews_url'),
    optTimeout: 90
  })
    .then('e70819', 'Core.Programming.Function', 'Shape The Facts', { func: '\nfunction textOf(v) {\n  if (!v) {\n    return \'\';\n  }\n  if (typeof v === \'string\') {\n    return v;\n  }\n  if (typeof v === \'object\') {\n    return v.name || v.title || v.label || v.text || \'\';\n  }\n  return String(v);\n}\n\n\nvar raw = msg.place_raw || {};\nvar place = raw.place_results || raw.place || raw;\n\nmsg.place_name = place.title || place.name || (msg.winner && msg.winner.name) || \'\';\nmsg.place_address = place.address || (msg.winner && msg.winner.address) || \'\';\nmsg.place_phone = place.phone || (msg.winner && msg.winner.phone) || \'\';\nmsg.place_rating = place.rating || (msg.winner && msg.winner.rating) || \'\';\nmsg.place_review_count = place.reviews || (msg.winner && msg.winner.reviews) || \'\';\n\nvar rawHours = place.opening_hours || place.operating_hours || place.hours || [];\nvar hours = [];\nvar i;\nif (rawHours && rawHours.length !== undefined && typeof rawHours !== \'string\') {\n  for (i = 0; i < rawHours.length; i++) {\n    var entry = rawHours[i];\n    if (!entry) {\n      continue;\n    }\n    if (typeof entry === \'string\') {\n      hours.push({ day: entry, hours: \'\' });\n      continue;\n    }\n    for (var k in entry) {\n      if (Object.prototype.hasOwnProperty.call(entry, k)) {\n        hours.push({ day: k, hours: textOf(entry[k]) });\n      }\n    }\n  }\n} else if (rawHours && typeof rawHours === \'object\') {\n  for (var d in rawHours) {\n    if (Object.prototype.hasOwnProperty.call(rawHours, d)) {\n      hours.push({ day: d, hours: textOf(rawHours[d]) });\n    }\n  }\n}\nmsg.place_hours = hours;\n\nvar rawCats = place.type || place.categories || place.types || [];\nif (typeof rawCats === \'string\') {\n  rawCats = [rawCats];\n}\nif (!rawCats.length && msg.winner && msg.winner.type) {\n  rawCats = [msg.winner.type];\n}\nvar cats = [];\nfor (i = 0; i < rawCats.length; i++) {\n  var c = textOf(rawCats[i]);\n  if (c) {\n    cats.push(c);\n  }\n}\nmsg.place_categories = cats;\n\nvar rev = (msg.reviews_raw && msg.reviews_raw.reviews) || [];\nvar scored = [];\nfor (i = 0; i < rev.length; i++) {\n  var r = rev[i];\n  if (!r) {\n    continue;\n  }\n  var body = r.snippet || r.extracted_snippet || r.text || r.description || \'\';\n  if (typeof body === \'object\') {\n    body = body.original || body.translated || \'\';\n  }\n  body = String(body || \'\');\n  if (!body) {\n    continue;\n  }\n  var who = \'\';\n  if (r.user) {\n    who = r.user.name || \'\';\n  }\n  if (!who) {\n    who = r.reviewer || r.author_name || r.name || \'\';\n  }\n  scored.push({\n    reviewer: String(who || \'A Google reviewer\'),\n    rating: r.rating || 0,\n    text: body,\n    date: r.iso_date || r.date || \'\'\n  });\n}\nscored.sort(function (a, b) {\n  return (b.rating || 0) - (a.rating || 0);\n});\nmsg.best_reviews = scored.slice(0, 10);\n\nmsg.photo_query = \'leather shoes\';\n\nmsg.photos_url = \'https://api.openverse.org/v1/images/\' +\n  \'?license=cc0\' +\n  \'&extension=jpg\' +\n  \'&category=photograph\' +\n  \'&page_size=6\' +\n  \'&q=\' + encodeURIComponent(msg.photo_query);\nreturn msg;\n' })
    .then('f8192a', 'Core.Net.HttpRequest', 'Search Openverse Photos', {
    outBody: Message('photos_raw'),
    outStatus: Message('photos_status'),
    optMethod: 'get',
    optUrl: Message('photos_url'),
    optTimeout: 90
  })
    .then('092a3b', 'Core.Programming.Function', 'Assemble The Brief', { func: '\nvar allowed = \'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789 -&()\';\nfunction folderName(s) {\n  var out = \'\';\n  var t = String(s || \'\');\n  for (var i = 0; i < t.length; i++) {\n    var ch = t.charAt(i);\n    if (allowed.indexOf(ch) !== -1) {\n      out += ch;\n    } else {\n      out += \' \';\n    }\n  }\n  var parts = out.split(\' \');\n  var keep = [];\n  for (var j = 0; j < parts.length; j++) {\n    if (parts[j].length > 0) {\n      keep.push(parts[j]);\n    }\n  }\n  return keep.join(\' \');\n}\n\nvar results = (msg.photos_raw && msg.photos_raw.results) || [];\nvar photos = [];\nfor (var p = 0; p < results.length && photos.length < 4; p++) {\n  var im = results[p];\n  if (!im || !im.url) {\n    continue;\n  }\n  photos.push({\n    url: im.url,\n    title: im.title || \'\',\n    creator: im.creator || \'Unknown\',\n    license: String(im.license || \'\'),\n    source_page: im.foreign_landing_url || \'\'\n  });\n}\n\nmsg.shop_dir = folderName(msg.place_name);\nif (!msg.shop_dir) {\n  throw new Error(\'Could not derive a folder name from the shop name\');\n}\n\nmsg.brief = {\n  dir_name: msg.shop_dir,\n  name: msg.place_name,\n  phone: msg.place_phone,\n  address: msg.place_address,\n  rating: msg.place_rating,\n  review_count: msg.place_review_count,\n  categories: msg.place_categories,\n  opening_hours: msg.place_hours,\n  reviews: msg.best_reviews,\n  photo_query: msg.photo_query,\n  photos: photos\n};\nreturn msg;\n' })
    .then('1a3b4c', 'Core.Flow.End', 'Material Gathered', { sfPort: 0 });
});