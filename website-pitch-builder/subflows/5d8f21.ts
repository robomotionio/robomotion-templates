import { subflow, Credential, Custom } from '@robomotion/sdk';

subflow.create('Build The Site', (f) => {
  f.node('2b4c5d', 'Core.Flow.Begin', 'Begin', {})
    .then('3c5d6e', 'Core.Programming.Function', 'Write The Brief', { func: '\nvar b = msg.brief;\nvar lines = [];\n\nlines.push(\'You are building a single web page for a real shoe repair shop that has no website of its own.\');\nlines.push(\'Everything you need is in the JSON at the bottom of this message. It is the only source of truth you have.\');\nlines.push(\'\');\nlines.push(\'WHERE THE PAGE GOES\');\nlines.push(\'Create the folder named exactly\');\nlines.push(\'  \' + b.dir_name);\nlines.push(\'inside your workspace, and write the page to\');\nlines.push(\'  \' + b.dir_name + \'/index.html\');\nlines.push(\'with your write tool. Write that one file and nothing else. Do not create any other file or folder.\');\nlines.push(\'\');\nlines.push(\'WHAT GOES ON THE PAGE\');\nlines.push(\'1. The shop name.\');\nlines.push(\'2. The phone number as a big, unmissable tel: link - it is the whole point of the page.\');\nlines.push(\'3. The street address.\');\nlines.push(\'4. The opening hours. In the JSON these arrive as an array of objects that each carry a single day as their only key.\');\nlines.push(\'5. The services the shop offers - but ONLY the ones its own reviews below actually describe. Read the review text and name what the reviewers say was done. If the reviews do not describe a service, it does not go on the page.\');\nlines.push(\'6. Exactly four review quotes, reproduced word for word, each one credited to the reviewer by the name given.\');\nlines.push(\'7. All four photographs, used in the order they are given. The first one is the full-width hero at the top; the other three follow below. Do not reorder them, do not drop one, do not go looking for others. Reference them by their remote url. Credit each photograph with its creator and its licence, linking to its source page.\');\nlines.push(\'   The licence is a string the photo API returned. Print it EXACTLY as it appears in the JSON - same spelling, same case. Do not uppercase it, do not expand it into a longer name, do not add a version number, do not substitute a licence you think is more correct. If the JSON says cc0 then the page says cc0.\');\nlines.push(\'\');\nlines.push(\'WHAT YOU MUST NOT DO\');\nlines.push(\'Invent nothing. Every fact on the page has to be traceable to the JSON below.\');\nlines.push(\'No prices. No years in business, no founding date, no "serving the city since". No awards, no accreditations, no certifications.\');\nlines.push(\'No email address and no contact form - the phone and the address are the only ways to reach this shop.\');\nlines.push(\'Do not invent staff names, guarantees, turnaround times or slogans the reviews do not support.\');\nlines.push(\'Do not reword the four quotes. Word for word means word for word.\');\nlines.push(\'Do not guess or tidy up a licence string - it is data, not prose.\');\nlines.push(\'\');\nlines.push(\'HOW IT SHOULD LOOK\');\nlines.push(\'Make it genuinely handsome - the kind of page that makes the owner want it. Considered typography, a restrained palette, real whitespace, a clear hierarchy that puts the phone number where a thumb lands. It must read well on a phone as well as on a desktop.\');\nlines.push(\'\');\nlines.push(\'THE HARD CONSTRAINT\');\nlines.push(\'One self-contained file. All CSS inside a single style tag. No external stylesheets, no web fonts, no JavaScript, no frameworks. The photographs are the only remote references.\');\nlines.push(\'The finished index.html must be UNDER 10240 bytes. This is a one page pitch, not a brochure. After you write it, check its size, and if it is over the limit tighten the CSS and write it again until it fits.\');\nlines.push(\'\');\nlines.push(\'When you are done, reply with one short line naming the file you wrote and its size in bytes.\');\nlines.push(\'\');\nlines.push(\'THE SHOP\');\nlines.push(JSON.stringify(b, null, 2));\n\nmsg.query = lines.join(String.fromCharCode(10));\nmsg.session_id = \'\';\nreturn msg;\n' })
    .then('4d6e7f', 'Robomotion.DeepSeekAgent.Agent.DeepSeekAgent', 'Write The Page', {
    optApiKey: Credential({ vaultId: '_', itemId: '_' }),
    optCustomModel: Custom('deepseek/deepseek-v4-pro-0813'),
    optLearning: 'off',
    optMemorySync: 'off',
    optWorkspace: Custom('/home/robot/PitchSites')
  });
  f.node('5e7f80', 'Core.Programming.Function', 'Locate The Page', { func: '\nvar workspace = \'/home/robot/PitchSites\';\nmsg.site_dir = workspace + \'/\' + msg.shop_dir;\nmsg.page_path = msg.site_dir + \'/index.html\';\nmsg.screenshot_path = msg.site_dir + \'/screenshot.png\';\nmsg.csv_path = msg.site_dir + \'/outreach.csv\';\nmsg.page_url = \'file://\' + msg.page_path;\nreturn msg;\n' })
    .then('6f8091', 'Core.Flow.End', 'Site Built', { sfPort: 0 });

  f.edge('4d6e7f', 2, '5e7f80', 0);
});